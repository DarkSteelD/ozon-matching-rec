# E-CUP 2026 — матчинг товаров Ozon (Задача 1)

Командный репозиторий задачи «Матчинг товаров» соревнования Ozon E-CUP 2026
(Students): идентификация одинаковых товаров по **текстовой** информации из
карточек (дедупликация карточек). ODS-слаг: `e-cup-2026-matching`.

## Ключевые факты

Базовые факты зафиксированы 2026-08-09 по правилам и странице трека; данные и
baseline сняты со страницы задачи **2026-08-13** `[V]`.

| Параметр | Значение |
| --- | --- |
| Формат решения | код-контейнер |
| Входные данные | текстовая информация карточек — **без изображений** (в отличие от матчинга-2024: названия + атрибуты + image embeddings) |
| Постановка | **кандидатные пары даны**: на вход решению подаются новые товары и их пары; классификация пары, retrieval не нужен |
| Лимит исполнения | **20 минут** на полный прогон решения (baseline укладывается в 18.5) |
| Метрика | **PR-AUC** (`total_prauc` в API лидерборда, снято 2026-08-13; слаг «total» намекает на пул, а не per-category 2024 года — перепроверить) |
| Окно посылок | по `2026-08-30 23:59 MSK`, 5 сабмитов/день, сброс 00:00 MSK |
| Финальные решения | выбрать ≤2 до `2026-08-31 12:00 MSK`, иначе платформа выберет сама |

Правило, влияющее на дизайн: переразметка данных LLM разрешена, но **только
моделями с открытой лицензией**; проприетарные LLM запрещены. Код обучения и
разметки должен быть воспроизводимым.

## Данные `[V 2026-08-13, страница задачи]`

| Файл | Размер | Содержимое |
| --- | --- | --- |
| `matches.parquet` | 4.1 MB | пары товаров с **ручной** разметкой |
| `matches_llm.parquet` | 104.7 MB | пары товаров с разметкой **LLM** |
| `items.parquet` | 4.1 GB | полные данные о товарах |
| `items_human.parquet` | 214.2 MB | подмножество товаров, встречающихся только в ручной разметке |

Глоссарий `matches*.parquet`: `id1`, `id2` — идентификаторы товаров пары;
`target` — метка совпадения: **0/1 в ручной разметке, от 0 до 1 в LLM-разметке**
(мягкие метки — кандидат на дистилляцию/взвешивание, но проверить их сдвиг
относительно ручных).

Глоссарий `items*.parquet`: `id`, `name` (название), `attributes` (свернутый
JSON атрибутов текстом), `category` (категория товара).

Все товары `items_human.parquet` содержатся в `items.parquet`; эта пара файлов
эмулирует прогон решения локально. **Суммарный объём тестовых данных сопоставим
с `matches.parquet` + `items_human.parquet`** — т.е. тест по масштабу равен
ручной разметке, и в тестовых данных о товарах есть только участники пар.

## Baseline и временной envelope `[V 2026-08-13]`

- `matching-baseline-submit.zip` (1.2 GB) — валидное решение на
  `cross-encoder/ms-marco-MiniLM-L12-v2` (HuggingFace); полный прогон
  **18.5 минут при лимите 20** — это ориентир максимально допустимой тяжести
  модели: MiniLM-L12 cross-encoder ≈ потолок по времени.
- `matching-baseline-lightweight.zip` (19.6 KB) — тот же код без модели, для
  отладки.
- **Все модели и артефакты должны быть включены в архив и/или образ** решения.

## Статус вопросов первого дня

1. ~~Даны ли candidate pairs~~ — **решено: даны** `[V]`; пайплайн = pair
   classification, как в 2024.
2. **Контейнерный envelope** — лимит времени известен (20 мин `[V]`); CPU, RAM,
   GPU/VRAM — уточнить по документации проверяющей системы. **Контракт запуска
   снят с `matching-baseline-lightweight.zip`** `[V 2026-08-13]`:
   - `metadata.json`: `{"image": "odsai/ecup26-matching-baseline:1.0",
     "entry_point": "python -u run.py"}` — образ + одна команда;
   - `run.py` получает CLI-аргументы `--items_path` (parquet: `id`, `name`,
     `attributes`, `category`), `--matches_path` (parquet: `id1`, `id2` —
     **без target**) и `--output_path`;
   - выход — CSV `id1,id2,predict` (вероятность/скор пары);
   - все модели — локальными файлами внутри архива (в lightweight лежит
     `baseline_logreg_l12.joblib`, полный дополнительно несёт
     `models/cross-encoder-ms-marco-MiniLM-L12-v2`); сети нет;
   - код бейзлайна: CE-эмбеддинги CLS-токена пар текстов «Name: … Category: …
     Attributes: …» → LogReg; распакован для инспекции в
     `members/darksteeld/baselines_ref/` (в Git не попадает — содержит веса).
3. ~~Метрика~~ — **решено: PR-AUC** (`total_prauc` `[V]`). Открыт только вопрос агрегации — прецедент 2024: скор считался
   **по категориям** (см. `members/darksteeld/reports/PUBLISHED_SOLUTIONS.md`),
   оптимизировать per-category, если подтвердится; evaluate считает обе
   агрегации (см. «Валидация»).
4. ~~Связь ручной и LLM-разметки~~ — **измерено `[V 2026-08-13]`: пересечение
   пустое.** Ни одного общего товара (0 из 12.4M/711K) и ни одной общей пары —
   прямая калибровка LLM→hand невозможна, зато обучение на `matches_llm` не
   течёт в фолды ручной валидации. Сдвиг LLM-меток оценивается только модельным
   экспериментом (train-on-LLM → eval-on-hand). Числа:
   `members/darksteeld/reports/DATA_EDA.md` §3.

## Валидация

Командный контракт зафиксирован: **grouped K=4** по компонентам связности
графа ручных пар (товар никогда не встречается в двух фолдах), метрика PR-AUC
в трёх агрегациях (mean по фолдам — первичная, pooled OOF, macro по
категориям). Полный контракт, SHA256-пины фолдов и команды —
`validation/README.md`; текущие результаты — `validation/leaderboard.md`.

```bash
make validation-targets                     # построить локальные цели фолдов из data/raw
make score MEMBER=<gh> EXPERIMENT=<slug> \
  NOTES="..."                               # оценить validation/predictions/<gh>/<slug>/fold_0K.csv
make leaderboard                            # пересобрать таблицу из results/*.json
```

Данные и выводы EDA (баланс, структура графа пар, LLM-разметка, категории,
тексты): `members/darksteeld/reports/DATA_EDA.md`. Реперные бейзлайны и их
place относительно публичного LB:
`members/darksteeld/reports/VALIDATION_BASELINES.md`.

## Структура репозитория

```text
data/
  raw/                    # единая неизменяемая копия данных задачи, не хранится в Git
members/
  darksteeld/             # рабочая область участника: evidence base исследований, далее код
  <github-name>/         # рабочая область следующего участника
validation/               # командный контракт валидации: замороженные фолды, evaluate, leaderboard
Makefile                  # score / leaderboard / leaderboard-md / validation-targets
```

Evidence base перенесена из мета-исследования `ozon-ecup-2026`:
`members/darksteeld/reports/FAMILY_MATCHING.md` — что реально двигает скор в
матчинге (Shopee, Amazon ESCI, WDC, Ditto и др., с числами), плюс общие отчёты
кампании (`00_SUMMARY.md`, `PRIOR_EDITIONS.md`, `PUBLISHED_SOLUTIONS.md`,
`OZON_NATIVE.md`).

## Правила работы команды

1. Не коммитить исходные данные, модели, секреты и файлы отправок.
2. Репозиторий приватный, публичных зеркал нет (правила соревнования, §2.12).
3. Создавать отдельную ветку для каждой гипотезы или задачи; в pull request
   описывать схему валидации и изменение метрики.
4. Переносить полезную логику из ноутбуков в `src/` своей рабочей области.
5. Оракульный потолок лейна и noise floor канала оценки измеряются **до**
   строительства лейна (см. `members/darksteeld/CAMPAIGN_RULES.md`).
6. Контракт валидации фиксируется всей командой в `validation/` и не меняется
   под конкретную модель.

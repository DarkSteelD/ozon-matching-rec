# Matching: две последние ODS-посылки `mb_stack`

Срез ODS: 2026-08-25 22:25 MSK. Метрика — macro PR-AUC по 20 категориям.
Live leaderboard endpoint: команда `Roma Bazuka` занимает **15-е место из 133**
со score **0.5124546791** после 26 посылок.

| Посылка | ODS ID | Macro PR-AUC | Решение |
|---|---|---:|---|
| `mb_stack.zip` | `9ed1e4c6-2888-44d6-a8f5-c671d8d9fc85` | **0.5124546791** | `GO`, новый лучший из этой пары |
| `mb_stack_clip.zip` | `d6033aa2-9ab8-4f1f-9dc4-326c9eaceb08` | **0.4785309230** | `NO_GO`, −0.0339237561 |

## 1. `mb_stack` — 0.512455

Это rank-blend двух кросс-энкодеров:

- 75% — RuModernBERT-base, одна модель с усреднёнными весами четырёх фолдов, `max_len=224`;
- 25% — student E3 `rubert-base`, `max_len=384`;
- обе ветки используют `prio`-представление текста из фактического `models.json`;
- после смешивания рангов применяется уже проверенный штраф `×0.25` к fashion-парам с несовместимыми размерами;
- time guard — 17 минут: если вторая модель не помещается, контейнер fail-closed остаётся на первой.

Локальный прогноз прироста не был честно измерим именно для student E3: он обучен на всех фолдах. Вес 0.75/0.25 перенесли с честных LOFO-партнёров со схожей корреляцией; ожидаемый `≈+0.002` был сценарием, а не validation claim. На ODS стек дал `+0.006426436` к предыдущему `mb_soup` (`0.506028243 → 0.512454679`).

## 2. `mb_stack_clip` — 0.478531

Модели, веса, токенизаторы и все веса сетей побайтово те же. HTTP Range-аудит ODS ZIP показал, что различаются только:

- `run.py`: SHA256 `578f0892…f379f18 → 1bf11b19…05c696`;
- `artifact.json`: SHA256 `ac9ae178…400736e → 0ff06807…7438f6`.

Единственное содержательное изменение — hard clipping финального score:

- score `<0.05` превращается в `0`;
- score `>0.95` превращается в `1`.

Для PR-AUC это уничтожает порядок внутри больших tied-групп. Локально clipping уже был отрицательным (`−0.000968` macro), но всё равно был отправлен как диагностический. На ODS он ухудшил все 20 из 20 категорий и macro на `−0.033924`.

Самые большие потери:

| Категория | `mb_stack` | `clip` | Δ |
|---|---:|---:|---:|
| Детские товары | 0.551897 | 0.473062 | −0.078835 |
| Бытовая химия | 0.709130 | 0.643083 | −0.066047 |
| Хобби и творчество | 0.600309 | 0.535855 | −0.064454 |
| Канцелярские товары | 0.413131 | 0.361454 | −0.051677 |
| Товары для животных | 0.604914 | 0.557600 | −0.047314 |

Полная разбивка лежит в [`MB_STACK_CATEGORY_DELTAS.csv`](MB_STACK_CATEGORY_DELTAS.csv).

## Решение

Оставляем `mb_stack` как текущего кандидата из этой пары. Hard clipping закрываем: для ранговой метрики он создаёт ties и системно портит результат. Если нужна калибровка крайних значений, она должна сохранять строгий порядок scores; обычный monotone transform PR-AUC не изменит.

## Provenance

- ODS files: 604,642,124 и 604,643,058 bytes; ETag `2566cbee79ba0d274ab58793b75631d0` и `5f31ca26397f3f1f45bfbf7f475a5211`.
- Оба ZIP имеют одинаковые 12 members; кроме `run.py` и `artifact.json`, CRC/size всех members совпадают.
- Live quota после этих посылок: 2 сегодня, 79 всего.

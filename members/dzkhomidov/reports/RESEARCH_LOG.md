# Исследовательский лог — dzkhomidov

Все скоры — mean_prauc на замороженной 4-фолд валидации, если не сказано иное.
Критерий значимости: одинаковый знак дельты на всех четырёх фолдах.

## Итоговые числа (на 2026-08-20)

| что | локально | лб ОДС |
| --- | ---: | ---: |
| final_stack_all (0.94·CE-анс + 0.06·zs-LLM) | **0.85638** | не влезает в контейнер |
| ce_priodistill_single (одна модель) | 0.85171 | — |
| посылка: студент+prio-тексты + 0.5·mdeb160 | ≈0.851 (рецепт) | **0.49460**, 13/81 |

## Принятые решения (каждое проверено на фолдах)

1. **Масштаб CE**: tiny→base +0.21.
2. **LLM-претрен** на 11.2М мягких пар (вселенная disjoint) +0.038; 2-я эпоха +0.002.
3. **Длина контекста** 128→384: +0.017 (самый надёжный рычаг).
4. **Кросс-архитектурный ансамбль** (rubase/e5/mdeb): +0.006–0.008.
5. **zero-shot LLM в стек** (вес 0.06): +0.0009, плюс на всех фолдах.
6. **Дистилляция**: студент rubase224 на 0.7·final_stack_OOF + 0.3·hard = **+0.0081** — одна
   модель уровня 3-арочного бленда. `src/train_final.py --data hand_pairs_distill.parquet`.
7. **Prio-тексты**: атрибуты бренд→модель/артикул→цвет (вместо алфавита), фэшн без размерных
   атрибутов, символьный блок `@@ сравнение: цвет=X; артикул=Y` в тексте пары: +0.0019 общий,
   Обувь +0.016, Одежда +0.021, macro_cat +0.004. Билдеры: `container/run_v3.py`.

## Отвергнутые гипотезы (не повторять)

- **Same-arch бэггинг**: +0.0005.
- **LGBM-стек vs ранк-анс**: равны (±0.0003).
- **KNRM в стек**: максимум +0.00006 (вес 0.01), знак по фолдам гуляет. knrm_name_v2
  воспроизведён (OOF 0.53097 ≈ 0.52987 у darksteeld); колонка в preds-паркете.
- **Авто-флип 1336 спорных меток + переобучение**: −0.0014 на всех фолдах, на чистых парах ≈0.
  Флипы должен подтверждать человек → `relabel/label_queue_v2.csv`.
- **Отдельная фэшн-CE** (78К фэшн-пар, prio+distill): хуже генералиста во всех 4 категориях
  (−0.006…−0.018), бленд тоже не спасает — мало данных, теряется кросс-категорийный перенос.
- **Фэшн-промпты для zero-shot LLM** (конвенция карточки / строгая с размером / пошаговая):
  все хуже генерик-промпта на локальном фэшн-сэмпле (0.281–0.306 vs 0.319).

## Конверт ОДС (измерено посылками)

- Формат: zip c `run.py --items_path --matches_path --output_path` → csv `id1,id2,predict`;
  образ `odsai/ecup26-matching-baseline:1.0`; сети нет; **20 минут** wall-clock, включая старт.
- **total_prauc = МАКРО-среднее по 20 категориям** (сошлось с среднее(per_category_prauc)).
- Таймер жёсткий: 3-арочный бленд с бюджетом 17.5 мин получил status=error (скор в metrics
  есть, но в лб НЕ идёт). Рабочий бюджет — 15.5 мин + деградация max_len + тайм-гард
  (`container/run_v2.py`/`run_v3.py`).
- Шкала: лб ≈ 0.55–0.58× локального, **порядок решений сохраняется**.

## Открытая проблема: фэшн на тесте

Тест: Обувь 0.094, Одежда 0.107 (локально 0.58/0.58) — уровень случайного скора, наши
локальные фэшн-приросты не переносятся. Рабочая гипотеза: у теста другая конвенция матча
(размер различает товары?). Дешёвая проверка — зонд за 1 посылку со штрафом за несовпадение
размера в 4 фэшн-категориях.

## Артефакты

- `preds/all_model_predictions_oof.parquet` — OOF 14 моделей × 365654 пар (актив-лернинг).
- `relabel/label_queue_v2.csv` — 1472 пары на доразметку, фэшн сверху.
- `src/` — тренировка (train_final, train_hand_fast с --data), zero-shot LLM скоринг
  (zs_llm_hf с --prompt-json), скоринг/сабмит; `container/` — код посылок; `prompts/` —
  фэшн-промпты; `reports/matching_track_report.pdf` — презентация (34 слайда).
- Чекпоинты и полные данные — на боксе в `~/matching-work/` (ckpt/, data/, preds/).
